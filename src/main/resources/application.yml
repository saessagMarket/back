# 로컬 환경
spring:
  config:
    import: workflow.yml
  datasource:
    url: jdbc:mysql://localhost:3306/saessagdb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: '0000'
  jpa:
    hibernate:
      ddl-auto: update

